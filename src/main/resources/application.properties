spring.datasource.url=jdbc:mysql://localhost:3306/userinfo
spring.datasource.username=root
spring.datasource.password=gossadmin
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configure Hibernate dialect for MySQL
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQLDialect

# This is to see the sql queries how are they generated by the JPA ....
spring.jpa.show-sql=true
jdbc:mysql://your-mysql-host:3306/your-database?useSSL=false



logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE



# Setting Logger levelsss, We can set for a particular package or classss..........
# logging.level.Udemy.course.controller = trace

# To a Particular class ......
logging.level.Udemy.course.controller.StudentController = debug

# By this we will get the log as a file in the root Folder and open with any text editor ..... 
# Properly place the logs inside the classes and packagesss.
#  We can adjust the file size, days , ... etc .....


logging.file.name = app.log
logging.file.



